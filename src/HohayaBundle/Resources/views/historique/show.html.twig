{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ssmenu</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sSMenu.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sSMenu.titre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ssmenu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ssmenu_edit', { 'id': sSMenu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
